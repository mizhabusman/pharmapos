# PharmaPOS backend dependencies (direct, curated).
# Regenerate a full lock with: pip freeze > requirements.lock.txt

# --- Web framework / server ---
fastapi==0.138.0
uvicorn==0.49.0
python-multipart==0.0.31      # multipart form parsing for file uploads
python-dotenv==1.2.2          # load backend/.env

# --- Data validation ---
pydantic==2.13.4

# --- Authentication (JWT) ---
PyJWT==2.10.1

# --- Inventory data + fuzzy search ---
pandas==3.0.3
rapidfuzz==3.14.5

# --- AI prescription extraction ---
google-generativeai==0.8.6

# --- Image preprocessing ---
pillow==12.2.0
